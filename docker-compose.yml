version: '3.8'

services:
  moneyforward-collector:
    image: book000/moneyforward-collector:v2.2.0
    volumes:
      - type: bind
        source: ./data
        target: /data
      - type: bind
        source: ./config.json
        target: /app/config.json
